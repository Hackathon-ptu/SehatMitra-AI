# Base URL of the FastAPI backend, including the /api/v1 suffix.
# Local development falls back to http://127.0.0.1:8000/api/v1 automatically,
# so this only needs setting for deployed environments (Vercel).
VITE_API_BASE_URL="https://sehatmitra-ai.onrender.com/api/v1"
